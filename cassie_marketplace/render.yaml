services:
  - type: web
    name: cassie-marketplace-backend
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
    startCommand: python manage.py migrate && gunicorn core.wsgi:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.5
